# Phase 3 Fixer Log

Timestamp: 2026-06-02T11:36:23Z

## Scope

Read `agents/fixer.md`, the Phase 3 critical review, the Phase 3 plot
validation report, `SELECTION.md`, Phase 3 scripts, machine-readable outputs,
`experiment_log.md`, and `methodology/appendix-plotting.md`. Addressed only
Phase 3 review findings and the additional luminosity-handling requirement.

## A1 Modelling Remediation

Restated finding: the primary `m_vis` and add-MET validation used a mixed
control/validation union with alarm-level data-vs-background-MC shape
`chi2/ndf`, without remediation attempts.

Changed `model_inputs_and_mva.py` to write `validation_remediation` into
`variable_modeling.json`. The loop records the original mixed union, separate
region checks, coarse rebinning, and a Z-rich-window visible-mass check.

Verification: final JSON reports mixed-union `m_vis` `chi2/ndf = 17.30` and
`m_addmet = 43.38`; separate-region visible-mass checks are 3.28 same-sign,
5.44 top handle, 2.24 W high-mT, and 1.17 Z-rich; coarse union does not fix
the broad mismatch; Z-rich-window `m_vis` is 1.17. `SELECTION.md` now states
the raw broad background templates are not closure-validated final Phase 4 fit
predictions and lists Phase 4 blockers.

Neighborhood check: verified the MVA downscope remains based on 13/16 failed
inputs and that the remediation table does not promote a classifier.

## A2 Region Semantics

Restated finding: `selected_events.npz` overwrote low-mT signal membership with
diagnostic validation/control labels.

Changed `build_selection.py` to add `is_signal_region`, `region_exclusive`,
`is_z_rich`, `is_top_btag_handle`, `is_w_high_mt`, `is_same_sign`, and
`is_same_sign_low_mt`. Updated `plot_selection.py` to use
`is_signal_region` for signal-region diagnostics and boolean flags for CR/VR
figures.

Verification: consistency check passes for data, signal, and background:
`is_signal_region` equals both `region_yields.json` signal counts and summed
`category_yields.json` counts. Exclusive labels no longer overwrite signal
membership.

Neighborhood check: checked all signal-region plots and category/template
plots use the same category or signal-region fields as the JSON counts.

## B1 Approach Comparison

Restated finding: the raw MC-only metric was presented too strongly as the
Phase 1 [D9] expected-sensitivity gate.

Changed `compare_approaches.py`, `approach_comparison.json`, plot label, and
`SELECTION.md` to call it diagnostic-only. The formal expected-sensitivity
gate remains a Phase 4 task requiring normalization, nuisance parameters, and
a statistical model.

Verification: `approach_comparison.json` reports
`expected_sensitivity_gate_status = not_evaluated_in_phase3`.

Neighborhood check: searched the Phase 3 artifact for stale wording claiming
that add-MET failed the formal expected-sensitivity gate.

## B2 Control-Region Counts

Restated finding: `SELECTION.md` had stale W high-mT and same-sign background
counts.

Updated `SELECTION.md` from `region_yields.json`: W high-mT is 4211 data and
6552 raw background MC; same-sign QCD sideband is 3227 data and 1240 raw
background MC.

Verification: direct JSON summation reproduces those values.

Neighborhood check: checked the Z-rich counts still agree with JSON.

## Plot Category A Fixes

Restated finding: plots used abbreviated open-simulation labels, risky legend
placement, edge-pinned categorical points, and mechanical variable labels.

Changed `plot_selection.py` to use `Open Data + Open Simulation`, add y-axis
headroom after legends, use padded x-limits for the approach comparison, and
render publication-quality MVA input labels.

Verification: `pixi run lint-plots` passed. Visually inspected
`category_yields.png`, `approach_comparison.png`, and
`mva_input_modeling_chi2.png`.

Neighborhood check: searched for stale `Open Sim.` wording and stale plot
labels in Phase 3 source and artifact outputs.

## Luminosity Binding Check

Restated finding: integrated luminosity must not be computed from event counts
or back-calculated from MC/data.

Changed `build_selection.py` and regenerated `normalization_inputs.json` to
record `integrated_luminosity.status = unresolved_blocking_input` and
`no_circular_derivation = true`. Checked CERN Open Data record 12350 and the
localized Run2012B/C reduced ROOT-file metadata; no exact integrated luminosity
for the localized reduced files was found. `SELECTION.md` now states Phase 4
cannot quote normalized yields without an exact citable CMS/Open Data source.

Verification: JSON check confirms the blocker and no-circular flag.

Neighborhood check: searched Phase 3 text for luminosity-normalized claims.

## Commands

- `pixi run phase3-all`
- `pixi run phase3-all`
- `pixi run lint-plots`
- `pixi run py - <<'PY' ... selected-event consistency check ... PY`
- `pixi run py - <<'PY' ... luminosity JSON check ... PY`

