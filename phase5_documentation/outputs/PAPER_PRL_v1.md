# A CMS Open Data Diagnostic Search for H to Tau Tau in the Mu Tau_h Final State

This PRL-formatted draft reports the audit-corrected result. The primary
visible-mass plus QCD/fake fit gives `mu_hat = 0.438`,
`mu < 10.764` at 95% CLs, with median expected
limit `10.738`. The categorized BDT-score result gives
`mu_hat = 9.352` and `mu < 14.837`,
but remains diagnostic only because score-shape validation is flagged.
